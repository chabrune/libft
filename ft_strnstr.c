#include "libft.h"

char 	*strnstr(const char *big, const char *little, size_t len)
{
    size_t i;
    size_t j;

    i = 0;
    if((!*little && !*big) || !*little)
        return((char*)big);
    
}

#include <stdio.h>

int main(void)
{

}